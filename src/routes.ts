export const publicRoutes = {
    error: '/error',
    signIn: '/sign-in',
    signOut: '/sign-out',
    ssoCallback: '/api/sso/callback',
};

export const DEFAULT_ROUTE = '/';

export const routes = {
    home: '/',
};
